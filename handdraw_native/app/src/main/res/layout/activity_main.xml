<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/bg">

    <!-- Live camera feed (front camera) -->
    <androidx.camera.view.PreviewView
        android:id="@+id/viewFinder"
        android:layout_width="0dp"
        android:layout_height="0dp"
        app:layout_constraintTop_toBottomOf="@id/topBar"
        app:layout_constraintBottom_toTopOf="@id/bottomBar"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent" />

    <!-- Strokes + hand skeleton + cursor, drawn on top of the camera feed -->
    <com.handdraw.studio.DrawingView
        android:id="@+id/drawingView"
        android:layout_width="0dp"
        android:layout_height="0dp"
        app:layout_constraintTop_toTopOf="@id/viewFinder"
        app:layout_constraintBottom_toBottomOf="@id/viewFinder"
        app:layout_constraintStart_toStartOf="@id/viewFinder"
        app:layout_constraintEnd_toEndOf="@id/viewFinder" />

    <!-- ================= TOP BAR ================= -->
    <LinearLayout
        android:id="@+id/topBar"
        android:layout_width="0dp"
        android:layout_height="56dp"
        android:orientation="horizontal"
        android:gravity="center_vertical"
        android:background="@color/dark"
        android:paddingHorizontal="8dp"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent">

        <Button style="@style/ToolbarButton" android:id="@+id/btnUndo" android:text="Undo" />
        <Button style="@style/ToolbarButton" android:id="@+id/btnRedo" android:text="Redo" />
        <Button style="@style/ToolbarButton" android:id="@+id/btnClear" android:text="Clear" />

        <TextView
            android:id="@+id/statusText"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:gravity="center"
            android:text="NO HAND"
            android:textColor="@color/accent_red"
            android:textStyle="bold"
            android:textSize="12sp" />

        <Button style="@style/ToolbarButton" android:id="@+id/btnSave" android:text="Save"
            android:backgroundTint="@color/accent_green" />
    </LinearLayout>

    <!-- ================= BOTTOM BAR ================= -->
    <LinearLayout
        android:id="@+id/bottomBar"
        android:layout_width="0dp"
        android:layout_height="72dp"
        android:orientation="horizontal"
        android:gravity="center_vertical"
        android:background="@color/dark"
        android:paddingHorizontal="6dp"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent">

        <Button style="@style/ToolbarButton" android:id="@+id/btnColors" android:text="● Colors" />
        <Button style="@style/ToolbarButton" android:id="@+id/btnBrush" android:text="✎ Brush" />
        <Button style="@style/ToolbarButton" android:id="@+id/btnEraser" android:text="⌫ Eraser" />
        <Button style="@style/ToolbarButton" android:id="@+id/btnCanvasBg" android:text="▦ BG" />
        <Button style="@style/ToolbarButton" android:id="@+id/btnMinus" android:text="－"
            android:layout_width="44dp" android:layout_weight="0" />
        <Button style="@style/ToolbarButton" android:id="@+id/btnPlus" android:text="＋"
            android:layout_width="44dp" android:layout_weight="0" />
    </LinearLayout>

</androidx.constraintlayout.widget.ConstraintLayout>
